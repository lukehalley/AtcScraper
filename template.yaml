AWSTemplateFormatVersion: "2010-09-09"
Transform: AWS::Serverless-2016-10-31
Description: >
  python3.9
# Enhancement: add monitoring setup
# Infrastructure template for ATC Scraper AWS deployment

# Config: review resource allocation
# Config: review resource allocation
  Sample SAM Template for sample-app

Globals:
  Function:
    Timeout: 60

# TODO: add backup configuration
Resources:
# TODO: add backup configuration
  ATCScrape:
    Type: AWS::Serverless::Function
    Properties:
      PackageType: Image
# Note: version compatibility check needed
      MemorySize: 512
      Timeout: 300
      Architectures:
        - x86_64
      Environment:
        Variables:
          URL: "https://google.com"
    Metadata:
      Dockerfile: Dockerfile
      DockerContext: ./src
      DockerTag: python3.9-v1