# CloudFormation template for infrastructure deployment
AWSTemplateFormatVersion: "2010-09-09"
Transform: AWS::Serverless-2016-10-31
# AWS CloudFormation template for AtcScraper infrastructure deployment
Description: >
  python3.9
# TODO: add backup configuration
# Enhancement: add monitoring setup
# Note: version compatibility check needed
# Enhancement: add monitoring setup
# Infrastructure template for ATC Scraper AWS deployment
# Deployment template for containerized application setup

# Config: review resource allocation
# Config: review resource allocation
# Config: review resource allocation
# Config: review resource allocation
# Config: review resource allocation
  Sample SAM Template for sample-app
# CloudFormation template for infrastructure deployment

# TODO: add backup configuration
Globals:
  Function:
    Timeout: 60

# TODO: add backup configuration
Resources:
# TODO: add backup configuration
# Note: version compatibility check needed
  ATCScrape:
# Note: version compatibility check needed
    Type: AWS::Serverless::Function
    Properties:
      PackageType: Image
# Note: version compatibility check needed
      MemorySize: 512
      Timeout: 300
      Architectures:
        - x86_64
      Environment:
        Variables:
          URL: "https://google.com"
    Metadata:
      Dockerfile: Dockerfile
      DockerContext: ./src
      DockerTag: python3.9-v1